# LINE 城市任務 - City Quest

基於 Vue 3 + TypeScript + Vite + Pinia + Tailwind CSS 的 LINE 城市任務應用程式

## 專案簡介

LINE 城市任務是一個結合 AR 掃描、地圖導航和遊戲化任務的城市探索應用。用戶可以：

- 🗺️ **探索附近任務** - 查看並接受附近的城市探索任務
- 📱 **AR 掃描體驗** - 使用 AR 掃描完成任務挑戰
- 👥 **組隊協作** - 邀請好友一起完成多人任務
- 🏆 **獲得獎勵** - 完成任務獲得 LINE Points 和成就徽章
- 📍 **地圖導航** - 透過地圖查看任務位置和導航

## 技術棧

- **前端框架**: Vue 3 (Composition API + `<script setup>`)
- **TypeScript**: 完整的類型支援
- **狀態管理**: Pinia
- **路由**: Vue Router 4
- **樣式**: Tailwind CSS 4
- **建構工具**: Vite 7
- **地圖整合**: Google Maps API
- **測試**: Vitest + Vue Test Utils

## 快速開始

### 環境需求

- Node.js >= 20.19.0 或 >= 22.12.0
- pnpm (推薦) 或 npm
- Google Maps API Key ([申請連結](https://console.cloud.google.com/google/maps-apis))

### 1. 安裝依賴

```bash
pnpm install
```

### 2. 配置環境變數

複製 `.env.example` 建立 `.env` 檔案：

```bash
cp .env.example .env
```

在 `.env` 檔案中設定你的 Google Maps API Key：

```env
VITE_GOOGLE_MAPS_API_KEY=your_actual_api_key_here
```

### 3. 開發模式

```bash
pnpm dev
```

應用將在 `http://localhost:5173` 啟動

### 建置生產版本

```bash
pnpm build
```

建置檔案將輸出到 `dist` 目錄

### 預覽生產版本

```bash
pnpm preview
```

### 執行測試

```bash
pnpm test:unit
```

### 程式碼檢查

```bash
pnpm lint
```

## 專案結構

```
frontend/
├── src/
│   ├── assets/          # 靜態資源
│   │   └── styles/
│   │       └── globals.css    # 全域樣式
│   ├── components/      # 共用組件
│   │   ├── BottomNav.vue      # 底部導航欄
│   │   └── RarityBadge.vue    # 稀有度徽章
│   ├── stores/          # Pinia 狀態管理
│   │   ├── user.ts            # 用戶狀態
│   │   └── quest.ts           # 任務狀態
│   ├── views/           # 頁面組件
│   │   ├── Home.vue           # 首頁/登入頁
│   │   ├── Feed.vue           # 任務列表
│   │   ├── Map.vue            # 地圖頁面
│   │   ├── Profile.vue        # 個人資料
│   │   ├── Rewards.vue        # 獎勵頁面
│   │   ├── Quest.vue          # 任務詳情
│   │   ├── Scan.vue           # AR 掃描
│   │   ├── Team.vue           # 組隊大廳
│   │   └── AR.vue             # AR 展示
│   ├── router/          # 路由配置
│   │   └── index.ts
│   ├── App.vue          # 根組件
│   └── main.ts          # 入口文件
├── public/              # 公共靜態資源
├── index.html
├── vite.config.ts       # Vite 配置
├── tsconfig.json        # TypeScript 配置
└── package.json
```

## 主要功能

### 1. 用戶系統
- LINE 登入整合
- 個人資料管理
- 成就徽章系統
- 任務歷史記錄

### 2. 任務系統
- AI 生成任務劇情
- 難度分級 (簡單/中等/困難)
- 單人/多人任務類型
- 任務進度追蹤
- 距離和預估時間顯示

### 3. AR 掃描
- QR Code/標記掃描
- 實時掃描進度
- 3D 模型展示

### 4. 地圖功能
- 整合 OpenStreetMap
- 用戶定位
- 任務標記
- 路線導航

### 5. 社交功能
- 附近玩家顯示
- 組隊邀請
- LINE 分享整合

### 6. 獎勵系統
- LINE Points 累積
- 成就徽章收集
- 連續完成獎勵
- 稀有度系統 (Common/Uncommon/Rare/Epic/Limited)

## 路由結構

| 路徑 | 組件 | 說明 |
|------|------|------|
| `/` | Home.vue | 首頁/登入頁 |
| `/feed` | Feed.vue | 任務列表頁 |
| `/map` | Map.vue | 地圖頁面 |
| `/profile` | Profile.vue | 個人資料頁 |
| `/rewards` | Rewards.vue | 獎勵頁面 |
| `/quest/:id` | Quest.vue | 任務詳情頁 |
| `/scan/:id` | Scan.vue | AR 掃描頁 |
| `/team/:id` | Team.vue | 組隊大廳頁 |
| `/ar` | AR.vue | AR 展示頁 |

## 狀態管理

### User Store (stores/user.ts)
- `isLoggedIn`: 登入狀態
- `userName`: 用戶名稱
- `completedQuests`: 完成任務數
- `steps`: 今日步數
- `totalPoints`: 累積點數
- `achievements`: 成就列表
- `unlockedAchievements`: 已解鎖成就

### Quest Store (stores/quest.ts)
- `quests`: 任務列表
- `getQuestById(id)`: 根據 ID 獲取任務

## 開發指南

### 添加新頁面

1. 在 `src/views/` 創建新的 Vue 組件
2. 在 `src/router/index.ts` 添加路由
3. 如需狀態管理，在 `src/stores/` 創建 store

### 添加新組件

1. 在 `src/components/` 創建組件
2. 使用 `<script setup>` 和 TypeScript
3. 遵循 Vue 3 Composition API 最佳實踐

### 樣式規範

- 使用 Tailwind CSS utility classes
- 統一使用 `#06C755` 作為品牌綠色
- 響應式設計優先（mobile-first）

## 瀏覽器支援

- Chrome (最新版)
- Firefox (最新版)
- Safari (最新版)
- Edge (最新版)

## 注意事項

- 地圖功能需要用戶授權定位權限
- AR 掃描功能需要相機權限
- 建議使用 HTTPS 以啟用完整功能

## 授權

私有專案 - 保留所有權利

---

**© 2025 LINE 城市任務. All rights reserved.**
